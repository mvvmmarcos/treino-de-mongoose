<h1>Editar Produtos</h1>
<form action="/products/update" method="POST">
    <div class="form-control">
        <input type="hidden" name="id" value="{{product._id}}">
        <label for="name">Nome do produto</label>
        <input type="text" name="name" placeholder="Digite o nome do produto..." value="{{product.name}}">
    </div>
    <div class="form-control">
        <label for="image">Imagem do produto</label>
        <input type="text" name="image" placeholder="Insira a URL do produto" value="{{product.image}}">
    </div>
    <div class="form-control">
        <label for="price">Preço do produto</label>
        <input type="text" name="price" placeholder="Digite o preço do produto..." value="{{product.price}}">
    </div>
    <div class="form-control">
        <label for="description">Nome do produto</label>
        <textarea name="description" placeholder="Descrição do produto...">{{product.description}}</textarea>
    </div>
    <div class="form-control">
        <input type="submit" value="Atualizar" class="btn">
    </div>
</form>